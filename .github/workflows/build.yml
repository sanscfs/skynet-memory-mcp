name: build
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    uses: sanscfs/skynet-ci/.github/workflows/build.yml@main
    with:
      component: skynet-memory-mcp
    secrets: inherit
